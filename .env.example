# Root env for docker-compose. Copy to .env and fill in.
# (For local non-Docker dev, use backend/.env.development.local instead.)

NODE_ENV=production
BACKEND_PORT=3001
FRONTEND_PORT=3000

# BFF
BASE_URL_PREFIX=/api
SECRET_KEY=change-me-to-a-long-random-string
ORIGIN=http://localhost:3000
CREDENTIALS=true

# WSO2 (test). Same base URL for Citizen and Employee.
API_BASE_URL=https://api-test.sundsvall.se
CLIENT_KEY={{INSERT CLIENT_KEY}}
CLIENT_SECRET={{INSERT CLIENT_SECRET}}
MUNICIPALITY_ID=2281

# Mocked citizen users (BankID). Comma-separated, PARALLEL lists: the Nth
# personId is paired with the Nth personnummer. Name is fetched from Citizen.
CITIZEN_PERSON_ID={{PERSON_UUID_1}},{{PERSON_UUID_2}}
CITIZEN_PERSON_NUMBER={{PERSONNUMMER_1}},{{PERSONNUMMER_2}}
# Shared password the citizen must enter in the login dialog.
CITIZEN_LOGIN_PASSWORD=change-me

# Admin login via SAML against the fake SSO IdP (web-app-fake-sso-idp)
SAML_ENTRY_SSO=http://localhost:7000/sso
SAML_CALLBACK_URL=http://localhost:3001/api/saml/login/callback
SAML_LOGOUT_CALLBACK_URL=http://localhost:3001/api/saml/logout/callback
SAML_SUCCESS_REDIRECT=http://localhost:3000/admin/dashboard
SAML_FAILURE_REDIRECT=http://localhost:3000/admin
SAML_ISSUER=web-app-raddningstjansten
SAML_IDP_PUBLIC_CERT="-----BEGIN CERTIFICATE-----\n{{INSERT_IDP_PUBLIC_CERT}}\n-----END CERTIFICATE-----\n"
# Editor (full read+write) groups. Read-only viewers go in VIEWER_GROUP.
ADMIN_GROUP=Raddningstjansten-AVD-CHEFER,Raddningstjansten-AVD-EDITOR
VIEWER_GROUP=Raddningstjansten-AVD-VIEWER

# Test SSO (mocked handläggare). Shared password + MySQL user store. Leave the
# password empty to disable the Test-SSO button. The DB host "testsso-db" is the
# compose/Dokploy service below.
EMPLOYEE_LOGIN_PASSWORD=change-me
TESTSSO_DATABASE_URL=mysql://testsso:testsso@testsso-db:3306/testsso
# Credentials for the testsso-db service (must match TESTSSO_DATABASE_URL).
TESTSSO_DB_ROOT_PASSWORD=rootpw
TESTSSO_DB_NAME=testsso
TESTSSO_DB_USER=testsso
TESTSSO_DB_PASSWORD=testsso

# rtj-management (errand API, no WSO2 token). In Dokploy use the internal host.
RTJ_MANAGEMENT_BASE_URL=http://rtj-apiservicertj-kpgs6d:8080
RTJ_NAMESPACE=EGENSOTNING
RTJ_DEFAULT_ASSIGNEE=bsk-handlaggare
RTJ_PROCESS_DEFINITION=Hantera ansökan om egensotning

# Where frontend nginx proxies /api (compose service name)
BACKEND_URL=http://backend:3000
